"""
Compatibility wrapper for scripts.seeding.trigger_seeding
Maintains backward compatibility with existing references.
"""
import sys
from scripts.seeding.trigger_seeding import *  # noqa

# The trigger_seeding script executes on import, so we just import it

